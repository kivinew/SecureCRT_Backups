[project]
name = "securecrt-backups"
version = "0.1.0"
description = "QT реализация диагностики GPON."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyqt6>=6.11.0",
    "pywin32>=311",
]
[tool.pyright]
extraPaths = [".venv/Lib/site-packages"]
